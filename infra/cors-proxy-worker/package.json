{
  "name": "slammer-cors-proxy",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker that tunnels image fetches from CORS-blocked museum / archive CDNs for slammer.app.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
